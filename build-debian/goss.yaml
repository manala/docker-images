############
# Packages #
############

package:
  bsdtar:
    installed: true
  wget:
    installed: true
  curl:
    installed: true
  less:
    installed: true
  vim:
    installed: true
  make:
    installed: true

############
# Commands #
############

command:
  # Su exec
  su-exec:
    exit-status: 0
  # Dumb init
  dumb-init --version:
    exit-status: 0
    stderr:
      - dumb-init v{{ .Env.DUMB_INIT_VERSION }}

##########
# Groups #
##########

group:
  {{ .Env.GROUP_DEFAULT }}:
    exists: true
    gid:    {{ .Env.GROUP_ID }}

#########
# Users #
#########

user:
  {{ .Env.USER_DEFAULT }}:
    exists: true
    uid:    {{ .Env.USER_ID }}
    groups:
      - {{ .Env.GROUP_DEFAULT }}
    home:  /home/{{ .Env.USER_DEFAULT }}
    shell: /bin/bash
